{
  "manifest_version": 3,
  "name": "Privacy-Preserving Vision Agent",
  "version": "0.3.0",
  "description": "Sees the screen on-device via a real vision model (ONNX Runtime Web) and OCR (Tesseract.js), no DOM access, redacts sensitive content client-side, sends only sanitized context to a server agent.",
  "permissions": ["activeTab", "scripting", "storage", "offscreen"],
  "host_permissions": ["http://localhost:8000/*"],
  "background": {
    "service_worker": "background.js",
    "type": "module"
  },
  "action": {
    "default_popup": "popup.html"
  },
  "web_accessible_resources": [
    {
      "resources": ["models/*"],
      "matches": ["<all_urls>"]
    }
  ],
  "content_security_policy": {
    "extension_pages": "script-src 'self' 'wasm-unsafe-eval'; object-src 'self';"
  }
}
