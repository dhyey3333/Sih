{
  "name": "vision-detector",
  "version": "0.2.0",
  "description": "On-device UI element detection (ONNX) + OCR-based sensitive-text redaction (Tesseract.js) for SIH26171",
  "type": "module",
  "dependencies": {
    "onnxruntime-web": "^1.29.0",
    "tesseract.js": "^7.0.0",
    "tesseract.js-core": "^7.0.0"
  },
  "main": "vision-detector.js",
  "scripts": {
    "test": "node --check background.js && node --check content.js && node --check offscreen.js && node --check popup.js && node --check vision-detector.js && node --check ocr-redactor.js && echo 'all extension JS files: syntax OK'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
